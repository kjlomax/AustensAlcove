// check out your own file
// username
// Read list
// wish list
// import { ApiMessage } from '../interfaces/ApiMessage';
// import List from '../components/List';

// interface ViewProfileProps {
//      id: number,
//      name: string,
//      experiencedList: string[],
//      wishList: string[]
//      addToExperiencedList: (bookId: number) => Promise<ApiMessage>
//      deleteFromList: (bookId: number) => Promise<ApiMessage>
//     };

// const ViewProfile = () => ({id, name, experiencedList, wishList, addToExperiencedList, deleteFromList}: ViewProfileProps) => {
//     const handleAdd: MouseEventHandler<HTMLButtonElement> = async (event) => {
//       const bookToAdd = Number(event.currentTarget.value);
//       if (!isNaN(bookToAdd)) {
//         try {
//             const data = await addToExperiencedList(bookToAdd);
//         } catch (error) {
//             console.error(error);
//         }
//       }
//     };
// };













//     return (
//       <section>
//         <h1>View Profile</h1>
//         <List /> //read list
//         <List /> //recommended
//       </section>
//     );
//   };
  
//   export default ViewProfile;